#ifndef CODEX_COMPAT_SYS_RESOURCE_H
#define CODEX_COMPAT_SYS_RESOURCE_H

/* Minimal compatibility shim for PolyBenchC on Windows. The benchmark runtime
   includes <sys/resource.h>, but the default timing path used by this project
   does not require declarations from it. */

#endif
