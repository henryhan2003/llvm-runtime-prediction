#ifndef LLVM_RUNTIME_COMPAT_HOOKS_H
#define LLVM_RUNTIME_COMPAT_HOOKS_H

/* Rodinia's streamcluster inherits an optional PARSEC hooks include, but the
 * benchmark does not call the hooks unless PARSEC instrumentation is enabled. */

#endif
